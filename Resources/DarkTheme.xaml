<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

        <!-- ===== COLOUR PALETTE ===== -->

        <!-- Backgrounds -->
        <Color x:Key="ColorBackground">#FF07111A</Color>
        <Color x:Key="ColorSidebar">#FF07101A</Color>
        <Color x:Key="ColorSurface">#FF102233</Color>
        <Color x:Key="ColorCard">#FF132839</Color>
        <Color x:Key="ColorSeparator">#FF214057</Color>

        <!-- Accent — DSX-like electric blue -->
        <Color x:Key="ColorAccent">#FF00A8E8</Color>
        <Color x:Key="ColorAccentGlow">#FF7DDCFF</Color>
        <Color x:Key="ColorAccentDim">#3300A8E8</Color>

        <!-- Status -->
        <Color x:Key="ColorConnected">#FF36D67E</Color>
        <Color x:Key="ColorError">#FFE81123</Color>

        <!-- Text -->
        <Color x:Key="ColorTextPrimary">#FFF5F5F5</Color>
        <Color x:Key="ColorTextSecondary">#FFBED1DE</Color>
        <Color x:Key="ColorTextMuted">#FF7C97A9</Color>
        <Color x:Key="ColorTextHeading">#FFFFFFFF</Color>

        <!-- ===== BRUSHES ===== -->
        <SolidColorBrush x:Key="BrushBackground"
                         Color="{StaticResource ColorBackground}"/>
        <SolidColorBrush x:Key="BrushSidebar"
                         Color="{StaticResource ColorSidebar}"/>
        <SolidColorBrush x:Key="BrushSurface"
                         Color="{StaticResource ColorSurface}"/>
        <SolidColorBrush x:Key="BrushCard"
                         Color="{StaticResource ColorCard}"/>
        <SolidColorBrush x:Key="BrushSeparator"
                         Color="{StaticResource ColorSeparator}"/>
        <SolidColorBrush x:Key="BrushAccent"
                         Color="{StaticResource ColorAccent}"/>
        <SolidColorBrush x:Key="BrushAccentGlow"
                         Color="{StaticResource ColorAccentGlow}"/>
        <SolidColorBrush x:Key="BrushAccentDim"
                         Color="{StaticResource ColorAccentDim}"/>
        <SolidColorBrush x:Key="BrushConnected"
                         Color="{StaticResource ColorConnected}"/>
        <SolidColorBrush x:Key="BrushError"
                         Color="{StaticResource ColorError}"/>
        <SolidColorBrush x:Key="BrushTextPrimary"
                         Color="{StaticResource ColorTextPrimary}"/>
        <SolidColorBrush x:Key="BrushTextSecondary"
                         Color="{StaticResource ColorTextSecondary}"/>
        <SolidColorBrush x:Key="BrushTextMuted"
                         Color="{StaticResource ColorTextMuted}"/>
        <SolidColorBrush x:Key="BrushTextHeading"
                         Color="{StaticResource ColorTextHeading}"/>

        <!-- ===== PREMIUM GRADIENTS ===== -->
        <LinearGradientBrush x:Key="BrushBackgroundGradient"
                             StartPoint="0,0"
                             EndPoint="1,1">
                <GradientStop Color="#FF07111A"
                              Offset="0"/>
                <GradientStop Color="#FF0C1D2B"
                              Offset="0.50"/>
                <GradientStop Color="#FF143145"
                              Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrushSidebarGradient"
                             StartPoint="0,0"
                             EndPoint="0,1">
                <GradientStop Color="#FF071019"
                              Offset="0"/>
                <GradientStop Color="#FF091521"
                              Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrushCardGradient"
                             StartPoint="0,0"
                             EndPoint="1,1">
                <GradientStop Color="#FF173247"
                              Offset="0"/>
                <GradientStop Color="#FF102638"
                              Offset="1"/>
        </LinearGradientBrush>

        <RadialGradientBrush x:Key="BrushAccentHalo"
                             RadiusX="0.8"
                             RadiusY="0.8"
                             Center="0.2,0.2"
                             GradientOrigin="0.2,0.2">
                <GradientStop Color="#3300A8E8"
                              Offset="0"/>
                <GradientStop Color="#0000A8E8"
                              Offset="1"/>
        </RadialGradientBrush>

</ResourceDictionary>
